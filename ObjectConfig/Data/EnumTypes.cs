namespace ObjectConfig.Data
{
    public enum ApplicationRole { Viewer, Administrator }

    public enum UserRole { Anonym, Viewer, Administrator, GlobalAdministrator }

    public enum EnvironmentRole { TargetEditor, Editor }

    public enum TypeRole { SaveViewer, Viewer, SaveEditor, Editor }
}
